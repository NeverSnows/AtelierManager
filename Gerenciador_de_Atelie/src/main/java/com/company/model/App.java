package com.company.model;
import com.company.dao.EntityManager;
import org.hibernate.Session;
import org.hibernate.SessionFactory;
import org.hibernate.cfg.Configuration;

import javax.persistence.EntityManagerFactory;
import javax.persistence.Persistence;


public class App {
    public static void main(String[] args) throws Exception{
        /* try {

            EntityManager.getEM().getTransaction().begin();

            Usuario u1 = new Usuario("nomeusuario", "emaiol", "senha");

            EntityManager.getEM().persist(u1);

            EntityManager.getEM().getTransaction().commit();

            EntityManager.getEM().close();
            EntityManager.getEM().close();
        }
        catch (Exception e) {
            e.printStackTrace();
        }

             */
    }
}
